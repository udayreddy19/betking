/**
 * OddsEngineV3 — RiskCalculator (Stub)
 * 
 * Intentionally NOT implemented in V3 Phase 1.
 * Risk/liability adjustment will be added after core pricing is verified.
 * 
 * This file exists to maintain the architecture contract.
 */

/**
 * No-op risk adjustment. Returns input unchanged.
 * @param {number} odds
 * @returns {number}
 */
export function applyRiskAdjustment(odds) {
  return odds;
}
